# Система управления объявлениями для модерации

Веб-приложение для модерации объявлений на платформе Авито. Упрощенная версия внутренней системы, которую используют модераторы для проверки и управления объявлениями пользователей.

## Быстрый старт
Откройте терминал(cmd)

# Сначала требуется скачать репозиторий

```bash
git clone https://github.com/Komissar4ik/Frontend-trainee-assignment-autumn-2025.git
```

# Перейти в папку

```bash
cd Frontend-trainee-assignment-autumn-2025
```

### С Docker (рекомендуется)
> **Требуется:** [Docker Desktop](https://www.docker.com/products/docker-desktop/)

# Для новых версий Docker
```bash
docker compose up --build
```

# Для старых версий
```bash
docker-compose up --build
```

### Без Docker
# Backend
```bash
cd tech-int3-server && npm install && npm start
```

# Frontend (в новом терминале)
```bash
npm install && npm run dev
```

**После запуска:**
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001

📖 [Полная инструкция по запуску](QUICKSTART.md) | [Docker документация](DOCKER.md)

## Технологии

### Обязательные требования

- **Node.js v20** - среда выполнения JavaScript
- **React v18+** - библиотека для построения пользовательского интерфейса
- **react-router-dom** - библиотека для маршрутизации в React-приложении
- **TypeScript** - типизированный надмножество JavaScript для повышения надежности кода
- **Docker** - для контейнеризированного запуска (опционально)

### Дополнительные технологии

- **Vite** - современный инструмент сборки, обеспечивающий быструю разработку и сборку проекта
- **Axios** - библиотека для выполнения HTTP-запросов с поддержкой отмены запросов
- **Recharts** - библиотека для построения графиков и диаграмм
- **date-fns** - библиотека для работы с датами и временем
- **ESLint** - инструмент для статического анализа кода
- **Prettier** - инструмент для автоматического форматирования кода
- **Jest** - фреймворк для тестирования JavaScript/TypeScript кода
- **React Testing Library** - библиотека для тестирования React компонентов

## Обоснование выбора технологий

### TypeScript

Выбран для обеспечения типобезопасности кода. Позволяет выявлять ошибки на этапе разработки, улучшает читаемость кода и упрощает рефакторинг. Строгая типизация особенно важна при работе с API и сложными структурами данных.

### Vite

Выбран вместо Create React App и Webpack из-за значительно более быстрой сборки и горячей перезагрузки модулей (HMR). Vite использует нативные ES-модули в разработке, что обеспечивает мгновенную загрузку страниц.

### Axios

Выбран для работы с HTTP-запросами из-за удобного API, поддержки отмены запросов через CancelToken и встроенной обработки ошибок. Это критично для реализации требования об отмене запросов при переходе между страницами.

### Recharts

Выбран для построения графиков статистики. Библиотека основана на D3.js, но предоставляет более простой React-ориентированный API. Обеспечивает адаптивность графиков и хорошую производительность.

### date-fns

Выбран для работы с датами из-за модульной структуры (позволяет импортировать только нужные функции), поддержки локализации и простого API. Альтернатива moment.js, но с меньшим размером бандла.

### ESLint и Prettier

Выбраны для обеспечения единообразия кода и соответствия критериям качества. ESLint проверяет код на ошибки и нарушения best practices, Prettier автоматически форматирует код согласно установленным правилам.

### Jest и React Testing Library

Jest выбран как основной фреймворк для тестирования из-за простоты настройки, встроенной поддержки моков и покрытия кода. React Testing Library используется для тестирования компонентов с фокусом на пользовательское поведение, а не на внутреннюю реализацию.

## Установка и запуск

### Предварительные требования

- Node.js v20 или выше
- npm или yarn
- Docker и Docker Compose (для контейнеризированного запуска)

### Запуск с Docker Compose (рекомендуется)

> **Требуется установка:** [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)

Самый простой способ запустить проект - использовать Docker Compose:

```bash
# Для новых версий Docker (Docker Desktop 4.0+)
docker compose up --build

# Для старых версий (если команда выше не работает)
docker-compose up --build
```

Эта команда:
- Соберет образы для фронтенда и бэкенда
- Запустит оба сервиса в контейнерах
- Настроит сеть между ними

После запуска:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001

Для запуска в фоновом режиме:

```bash
docker compose up -d --build
```

Для остановки:

```bash
docker compose down
```

Для просмотра логов:

```bash
docker compose logs -f
```

#### Запуск в режиме разработки (с hot reload)

Для разработки с автоматической перезагрузкой при изменении кода:

```bash
docker compose -f docker-compose.dev.yml up --build
```

В этом режиме:
- Изменения в коде автоматически применяются без пересборки образа
- Frontend использует Vite dev server с HMR
- Backend использует nodemon для автоматической перезагрузки

### Локальный запуск (без Docker)

#### Установка зависимостей

```bash
npm install
```

#### Запуск сервера API

В отдельном терминале:

```bash
cd tech-int3-server
npm install
npm start
```

Сервер API будет доступен по адресу `http://localhost:3001`

#### Запуск клиентского приложения

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist`

### Просмотр production сборки

```bash
npm run preview
```

## Тестирование

### Запуск тестов

```bash
npm test
```

### Запуск тестов в режиме watch

```bash
npm run test:watch
```

### Запуск тестов с покрытием

```bash
npm run test:coverage
```

Тесты находятся в папке `src/__tests__` и организованы по типам:
- `utils/` - тесты утилитарных функций
- `hooks/` - тесты пользовательских хуков
- `components/` - тесты компонентов
- `api/` - тесты API клиента

## Структура проекта

```
src/
├── __tests__/          # Unit тесты
├── api/                # API-клиент и настройка HTTP-запросов
├── components/         # Переиспользуемые UI-компоненты
├── hooks/              # Пользовательские React-хуки
├── pages/              # Страницы приложения
├── styles/             # Глобальные стили
├── test/               # Настройка тестового окружения
├── types/              # TypeScript типы и интерфейсы
└── utils/              # Вспомогательные функции
```

## Функциональность

### Страница списка объявлений (/list)

- Отображение объявлений в виде карточек
- Фильтрация по статусу, категории, диапазону цен
- Поиск по названию объявления
- Сортировка по дате, цене, приоритету
- Пагинация (10 объявлений на страницу)
- Синхронизация фильтров с URL
- Bulk-операции (массовое одобрение/отклонение)
- Сохранение наборов фильтров
- Real-time обновления

### Страница детального просмотра (/item/:id)

- Полная информация об объявлении
- Галерея изображений
- Характеристики товара
- Информация о продавце
- История модерации
- Панель действий модератора (одобрить, отклонить, вернуть на доработку)
- Навигация между объявлениями
- Горячие клавиши (A, D, <-, ->)

### Страница статистики (/stats)

- Общая статистика (всего проверено, проценты одобренных/отклоненных, среднее время проверки)
- График активности по дням
- Круговая диаграмма распределения решений
- График распределения по категориям
- Фильтрация по периоду (сегодня, неделя, месяц)
- Экспорт статистики в CSV и PDF

## Особенности реализации

### Отмена запросов

Реализована система отмены HTTP-запросов при переходе между страницами. Используется Axios CancelToken для предотвращения утечек памяти и ненужных обновлений состояния компонентов.

### Типизация

Все компоненты, функции и данные полностью типизированы с использованием TypeScript. Типы API соответствуют схеме OpenAPI из файла `schema.yaml`.

### Оптимизация производительности

- Использование `useCallback` для мемоизации функций
- Отмена запросов при размонтировании компонентов
- Оптимизация рендеринга списков через ключи React
- Skeleton loading для предотвращения тряски страницы

### Качество кода

- Строгое соблюдение правил ESLint
- Автоматическое форматирование через Prettier
- Говорящие имена переменных и функций
- Отсутствие дублирования кода
- Минимизация сложности функций
- Покрытие кода unit-тестами

### Темная тема

Реализована поддержка темной темы с переключателем в header. Выбор темы сохраняется в localStorage и применяется автоматически при загрузке страницы.

## Лицензия

MIT
